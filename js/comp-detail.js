import{setupLanguage,t}from'./i18n.js';import{detail}from'./ui.js';const slug=new URLSearchParams(location.search).get('slug');let comp;function render(){document.querySelector('#comp-detail').innerHTML=comp?detail(comp):`<p class="empty">${t('notFound')}</p>`}fetch('data/comps.json').then(r=>r.json()).then(xs=>{comp=xs.find(x=>x.slug===slug)||xs[0];document.title=`${comp.name.en} — MokaTroy TFT`;render()});setupLanguage(render);
